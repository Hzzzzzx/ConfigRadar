package io.github.hzzzzzx.configradar.core.model;

/** Scan mode selected by the caller. Phase 1 implements STATIC_SOURCE. */
public enum ScanMode {
    STATIC_SOURCE,
    ARTIFACT,
    RUNTIME_SNAPSHOT
}
