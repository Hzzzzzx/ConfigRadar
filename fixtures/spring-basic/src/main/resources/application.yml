spring:
  application:
    name: spring-basic
  config:
    import: optional:configserver:http://localhost:8888,optional:classpath:imported.properties
  profiles:
    include: shared
server:
  port: 8080
  shutdown-grace-period: 30s
datasource:
  url: ${DB_URL:jdbc:postgresql://localhost:5432/app}
nested:
  url: ${NESTED_URL:${NESTED_HOST:-localhost}}
---
spring:
  config:
    activate:
      on-profile: prod
client:
  endpoint: https://api.example.com
